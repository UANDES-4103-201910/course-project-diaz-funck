<nav class="navbar fixed-top navbar-expand-md navbar-dark bg-primary">
  <div>
  <%= link_to 'GeoComplainer', home_index_path, :class => "navbar-brand text-white" %>
  </div>
  <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search">
      <%= link_to "Search", search_path, :class => 'btn btn-outline-light mx-2 text-light'%>
  </form>
  <%= form_tag(search_path, method: "get", :class => "form-inline my-2 my-lg-0") do %>
    <%= text_field_tag(:q) %>
    <%= submit_tag("Search", :class => 'btn btn-outline-light mx-2 text-light') %>
  <% end %>
  <ul class="navbar-nav ml-md-auto">
    <%= link_to "Sign Up", :register, :class => 'btn btn-outline-light text-light mx-2'%>
    <%= link_to "Log In", :login, :class => 'btn btn-outline-light text-light mx-2'%>
  </ul>
</nav>
